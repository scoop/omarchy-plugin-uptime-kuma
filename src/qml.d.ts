// These files are QML JavaScript resources, which have no module system: the
// shell loads them as plain scripts. The trailing `module.exports` guard exists
// only so the same file can be required by the test runner, so `module` is
// declared here rather than by pulling in a whole Node typings package.
declare var module: { exports: unknown } | undefined;
