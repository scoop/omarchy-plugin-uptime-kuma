// Conventional commits, which this repository already writes by hand. Enforcing
// them is what lets the history be read by something other than a person.
export default { extends: ["@commitlint/config-conventional"] };
